# Plan 07 codex L1 peer review — in-place fix log

- **Plan file**: `.forge/plans/2026-07/07-ps-shared-curation-and-convergence.md`
- **Peer review verdict**: `REQUEST_CHANGES` (codex L1, 10 findings: 4 HIGH + 5 MED + 1 LOW)
- **Peer review artifact**: `/private/tmp/claude-501/-Users-wukai-data-repos-github-the-alephain-guild-tesseract-trading-custos/5107788a-8761-4294-9d1d-9c4515183572/scratchpad/codex-peer/lastmsg-07.txt`
- **Fix cycle date**: 2026-07-09 (plan-drafter-07, in-place directed fix)
- **Plan file post-fix line count**: 553 (up from 542 pre-fix, net +11 from CR-2 table split + CR-6 red-line table expansion)

---

## Fix table

| ID | Severity | Location (pre-fix line) | Finding | Fix action | Verification |
|----|----------|-------------------------|---------|------------|--------------|
| CR-1 | HIGH | plan lines 21, 149, 315, 437, 447 | DP2 Docker evidence cites `deploy/nautilus/Dockerfile:59 COPY shared /app/shared`, but evidence-scout only anchors nautilus Dockerfile header + `:35 COPY shared/README.md` at scout §4.2 lines 131-134 — plan's line-59 assertion is not scout-verbatim | Replaced all 5 `Dockerfile:59` locations with scout-verbatim anchors: nautilus header `Dockerfile:1-8` ("runtime base: NT + deps + shared + sidecar") + `:35 COPY shared/README.md /tmp/deps/shared/README.md`; hummingbot `Dockerfile.image:28-29 COPY --chown=hummingbot:hummingbot shared /home/hummingbot/shared` + `:49 ENV PYTHONPATH=/home/hummingbot:/home/hummingbot/shared` (all scout §4.2 verbatim) | `grep -c 'Dockerfile:59' plan07` → 0 (was 5); scout-verbatim anchors now used consistently at all 5 locations |
| CR-2 | HIGH | plan line 29 (§Contract Verification Gate) | Table intro claims all anchors are scout quotations, but rows 42-45 cite drafter reads/greps, violating scout's file:line-only discipline + lesson C2 grep-evidence requirement | Split into two tables. **Table 1 — Scout-verbatim anchors**: rows for the vendored subset, drift check, hummingbot exclusion, tradingview exclusion, crucible Docker bundling, crucible import audit, ps consumers, ps HEAD, custos HEAD, and aion_trend/adaptive_grid non-consumer status (scout §1.2). **Table 2 — Additional drafter verification anchors (not scout-verbatim)**: rows for existing `TOOLKIT_PROVENANCE.md` content (drafter Read), `Makefile toolkit-sync-check` stub (drafter Read), `docs/design/nautilus_host.md` no-sync-section grep (drafter grep), and the drafter `find` re-check of `aion_trend` + `adaptive_grid` `refinement/` paths | `grep -c 'Table 1 — Scout-verbatim'` → 1; `grep -c 'Table 2 — Additional drafter verification anchors'` → 1; scout-anchor table + drafter-anchor table are visually distinct with separate intros |
| CR-3 | HIGH | plan lines 158, 194-197, 428-429, 440-441, 459-460, 471-472 | DP sections state "CEO ratifies" as if already ratified while DEV entries say "CEO pending" — execution ambiguity vs handoff packet §76-83 required CEO DP elevation | Normalized every DP status to the uniform state "**drafter recommends; CEO pending**" until execute-team dispatch records the CEO choice. Fixed 11 individual locations: DP1 Curation options intro + drafter recommend line; DP2 options intro + drafter recommend line; DP3 options intro + drafter recommend line; DP4 drafter recommend line; Key Design Decisions table 4 DP rows; and Goal section items 2 and 3. Also updated the Deviations section intro paragraph to declare "All DP status uniform 'drafter recommends; CEO pending' until execution dispatch records CEO choice (CR-3 fix)" | `grep -c 'CEO ratif' plan07` → 0 (was 8); `grep -c 'drafter recommends; CEO pending'` → 8 (uniform state at every DP location); no "CEO ratifies" / "CEO ratification" residual |
| CR-4 | HIGH | plan lines 397, 495, 530 | §Failure-mode says 8 candidate NEW tests but §Codex peer review + §Close-out claim 10 — anti-fabricated test-count discipline violated (lesson #25) | Reconciled to exactly **9 NEW tests** (one added via CR-5 fix — `test_toolkit_sync_check_requires_ps_root`). Rebuilt failure-mode table with numbered rows (1-9) covering exactly 9 NEW test names, plus an unnumbered T4.1 extension row (extends test #1 `test_toolkit_provenance_curation_decision_recorded`, not a separate NEW test) and one no-regression row (`test_vendored_toolkit_no_new_float_money_math`, ✓existing). Added an explicit "**Total NEW tests: 9**" summary sentence after the table. Updated §Codex peer review criteria (was "10 NEW tests") → "9 NEW tests + 1 no-regression". Updated §Close-out template (was "10 NEW contract tests") → "9 NEW contract tests" | `grep -c '9 NEW'` in plan → present in scope summary (line 9), close-out template, peer-review criteria, failure-mode table intro, and drafter grep re-verification note (5 hits, all consistent); no "10 NEW tests" residual; no "8 candidate NEW" residual (was 1 pre-fix); numbered rows 1-9 in failure-mode table |
| CR-5 | MED | plan lines 277, 389 | `PS_ROOT`-unset fail-fast case folded into `test_toolkit_sync_check_detects_upstream_drift` (synthetic drift fixture) — missing-env branch not independently testable (lesson #17/#22/#28) | Added dedicated test `test_toolkit_sync_check_requires_ps_root` (runs target with `PS_ROOT` unset, asserts non-zero exit + clear error message referencing `PS_ROOT`). Updated File Inventory row for `test_toolkit_sync_check.py` to add test (4) `test_toolkit_sync_check_requires_ps_root`. Updated Task T2.1 Step-1 to include the new test in the red-write step. Updated Task T2.2 Step-3 (Makefile upgrade) + failure-mode row to reference dedicated fail-fast per lesson #22/#28 independent-testability | `grep -c 'test_toolkit_sync_check_requires_ps_root'` in plan → 7 (File Inventory row + failure-mode table row 6 + T2.1 Step-1 + T2.2 Step-3 mention + T2.2 failure-mode line + peer-review criteria mention + close-out template); `grep -rn 'def test_toolkit_sync_check_requires_ps_root' tests/` → 0 hits (lesson #25 clean, executor creates in Track 2) |
| CR-6 | MED | plan line 407 (red-line 0.1/0.2 rows) | Red-line table uses `(existing gates)` placeholder for 0.1 and 0.2 — lesson #40 requires explicit code_coverage/runtime_wire/defer_status with file:line anchors | Replaced `(existing gates)` for both rows with concrete inherited test names + file:line anchors: **0.1**: `tests/test_credential_lifecycle.py` (grep-verified present) + `tests/test_credential_vault.py` + `tests/test_credential_vault_sops.py`; runtime wire = `src/custos/core/credential_vault.py` `_verify_permission_scope()` + telemetry actor whitelist per `docs/design/credential_vault.md` + `docs/design/telemetry_actor.md`. **0.2**: `tests/test_g6_gate.py` + `tests/test_g6_gate_capability_integration.py` + `tests/test_g6_gate_capability_e2e.py` (all grep-verified present); runtime wire = `NtTradingNodeHost.start()` gate at `src/custos/engines/nautilus/host.py` per `docs/design/nautilus_host.md` §"G6 gate contract". **0.4**: added anchor `tests/engines/nautilus/test_toolkit_provenance.py` (grep-verified 1 hit) + runtime wire = `src/custos/core/telemetry_actor.py` Decimal→str serialization per `docs/design/telemetry_actor.md` §money contract | `grep -c '(existing gates)' plan07` → 0 (was 2); every red-line row now names concrete `tests/<file>.py` + runtime-wire source file + doc anchor |
| CR-7 | MED | plan line 1 (title, headings, DEV entries, table columns) | Chinese in plan body, headings, table columns, DEV headings — packet §220-222 + `CLAUDE.md:147-164` + `code-style.md:104-119` require English-primary for plan file bodies + DEV IDs + test names + commit messages + JSON keys | Full body English sweep. Translated: title (`sync 纪律真实现` → `sync discipline real implementation`); Goal item 4 (`Sync 纪律 real implementation` → `Sync discipline real implementation`); DP3 section heading; Track 2 heading; File Inventory intro + all 4 table `现状(test -f)` column headers → `as-of (test -f)`; §Verification checklist (`§red-line专项` → `§"red-line specific check"`); every DEV entry (labels 等级/问题/影响/决定/CEO 拍板/更新的文档 → Severity/Question/Impact/Decision/CEO ruling/Updated documents); DEV 【CEO DECISION POINT N】 → [CEO DECISION POINT N]; `CEO 终裁 pending` → `CEO pending`; `禁 git add .` → `forbids git add .`; References row (`§执行顺序建议` → `execution-order suggestion`); scattered Chinese phrases like `无 destructive delete` → `no destructive delete`, `§下一步` → `"Next steps"` | `grep -cP '[\p{Han}]' plan07` → 0 (was 53); no Chinese remains in drafter-authored plan body |
| CR-8 | MED | plan line 139 + scout report line 62 | Plan repeats "`hummingbot` string absent from `src/custos/`" but direct grep finds `src/custos/core/engine_protocol.py:3` docstring + toolkit vendored refs; scout §1.3 makes the same overclaim | Restated as "no custos Hummingbot host implementation and no `shared.hummingbot.*` runtime consumer outside vendored/provenance references" at plan line 139 (Curation option (b)) + DEV-07-CURATION-SCOPE option (b) + Contract Verification Gate row 3. Named the exact grep hits inline: `src/custos/core/engine_protocol.py:3` (docstring naming the class of engines the protocol targets), toolkit `shared/config/validator.py`, toolkit `shared/nautilus/config/platforms.py`. Appended §11 errata note to evidence-scout report `.forge/handoff/2026-07/evidence-scout-07-08.md` naming the narrower framing correction with the corrected grep output | `grep -n 'no custos Hummingbot host implementation' plan07` → 3 hits (Curation section + Contract Verification Gate + DEV-07-CURATION-SCOPE option (b)); scout report §11 appended with errata timestamped 2026-07-09 |
| CR-9 | MED | plan lines 9, 361-373, 397, 495, 530 | multi_session rationale says `~10 tasks, ~4-6 NEW contract tests`, Progress table has 9 tasks, failure/close-out sections claim 8 or 10 NEW tests | Normalized to consistent counts across all sections: **5 tracks / 9 tasks / 9 NEW contract tests** (post-CR-5 addition). Updated scope summary at line 9; multi_session_scope statement at post-Progress-table line; failure-mode intro sentence; failure-mode table numbered rows 1-9 + summary sentence + total-NEW claim; peer-review criteria bullet 5; Close-out Report §"Failure-mode coverage" | Grep confirms consistency: line 9 says "5 tracks, 9 tasks, 9 NEW contract tests"; post-Progress line says "9 tasks total (across 5 Tracks)"; failure-mode intro says "total NEW tests = 9 (was 8; added `test_toolkit_sync_check_requires_ps_root`)"; peer-review criteria says "9 NEW tests + 1 no-regression"; close-out template says "9 NEW contract tests"; no "10 NEW" residual; no "8 candidate" residual; no "~10 tasks" or "~4-6 NEW" residual |
| CR-10 | LOW | plan line 270 (T1.2 commit message) | Hypothetical commit messages include `Plan 07` tracking label, contrary to plan's own no-tracking-number guidance at line 250 + code-style.md:102 ban on plan/task/lesson tracking references | Semantic phrasing applied: T1.2 commit `docs(custos): record Plan 07 curation decision in toolkit provenance` → `docs(custos): record toolkit curation decision in provenance`. T2.3 commit `docs(custos): add drift audit log to toolkit provenance (Plan 07 sync discipline)` → `docs(custos): add drift audit log to toolkit provenance sync discipline`. T5.1 commit `docs(custos): mark plan 07 as completed` retained (this matches the `progress-management.md` §"完成报告模板" `docs(custos): mark plan NN as completed` pattern — not lesson-#15 pollution). Also purged provenance section titles: `"Curation decision (Plan 07 landing)"` → `"Curation decision"`; `"pandas_ta governance (Plan 07 DP4 formalization)"` → `"pandas_ta governance"`. Fixed T1.2 red-step + green-step + File Inventory rows to match | **Corrected 2026-07-10 (L2-FU-07-1)**: the original verification command `grep -nE 'commit .*Plan 07\|feat.*Plan 07\|docs.*Plan 07\|test.*Plan 07'` mixes BRE-style escaped alternation (`\|`) into ERE mode (`-E`), which under ERE means a literal backslash-pipe character rather than alternation — the pattern therefore never matches anything, producing a trivially-true "0 hits" regardless of plan content. The corrected command (unescaped `\|` for ERE) is `grep -nE 'commit .*Plan 07|feat.*Plan 07|docs.*Plan 07|test.*Plan 07' .forge/plans/2026-07/07-ps-shared-curation-and-convergence.md`, which returns **6 hits** (plan lines 8, 63, 206, 418, 419, 421) — all contextual prose (e.g. "Plan 07 makes no source-code changes", "Plan 07 delivery = no-regression", "Plan 07 must not block Plan 08 start"), none of them an actual embedded commit-message string. The CR-10 fix's substantive claim (no `Plan 07` tracking label inside hypothetical commit messages or provenance section titles) still holds — verified independently by the executor's actual commit messages landed during Track 1-5 (none reference `Plan 07`) — but the original "0 hits" verification claim itself was produced by a broken grep, not a clean plan body. |

---

## Errata to evidence-scout report

- Scout §1.3 hummingbot "absent from `src/custos/`" overclaim — appended §11 errata to `.forge/handoff/2026-07/evidence-scout-07-08.md` (2026-07-09) with narrower verified framing "no custos Hummingbot host implementation and no `shared.hummingbot.*` runtime consumer outside vendored/provenance references" + grep output naming `src/custos/core/engine_protocol.py:3` docstring + toolkit `shared/config/validator.py` + toolkit `shared/nautilus/config/platforms.py` as the reference sites codex L1 CR-8 surfaced. This preserves the DP1 refutation logic (no runtime Hummingbot host in custos = no gain from expansion to `shared/hummingbot/`) while restating the evidence with grep-accurate scope.

---

## Post-fix invariants

- **CR-1 Dockerfile:59 remnants**: `grep -c 'Dockerfile:59' plan07` → 0 (was 5 pre-fix)
- **CR-2 Scout vs drafter anchors separated**: Table 1 (scout-verbatim) + Table 2 (drafter reads/greps) distinct headings + intros
- **CR-3 DP state uniform**: `grep -c 'CEO ratif' plan07` → 0 (was 8); every DP now says "drafter recommends; CEO pending"
- **CR-4/9 test-count normalized**: 9 NEW tests + 1 no-regression across all sections (scope summary + Progress + failure-mode + peer-review + close-out)
- **CR-5 dedicated fail-fast test added**: `test_toolkit_sync_check_requires_ps_root` at 7 plan locations; 0 hits in `tests/` (lesson #25 clean)
- **CR-6 red-line table concrete**: `(existing gates)` placeholder → concrete file:line anchors for 0.1, 0.2, 0.4 (0.3 = Plan 07 no-op inherited from Plan 04+06)
- **CR-7 English-primary**: `grep -cP '[\p{Han}]' plan07` → 0 (was 53); no drafter-authored Chinese remains
- **CR-8 hummingbot claim narrower**: "no custos Hummingbot host implementation or `shared.hummingbot.*` runtime consumer outside vendored/provenance references" at 3 plan locations + scout §11 errata
- **CR-10 semantic commit + section titles**: no "Plan 07" in commit-artifact strings or provenance section titles; contextual prose retains "Plan 07" as needed (e.g., "Plan 07 makes no source-code changes")
- **Test-name uniqueness (lesson #25)**: all 9 NEW test names grep-verified `hits=0` in `tests/` (recorded in drafter Bash log); `test_vendored_toolkit_no_new_float_money_math` grep-verified `hits=1` (at `tests/engines/nautilus/test_toolkit_provenance.py`)

## Grep evidence (lesson #25 test-name discipline)

Raw grep output from drafter Bash run on 2026-07-09, executed pre-fix (pre-CR-5) and re-run mid-fix (post-CR-5) with identical results:

```
test_toolkit_sync_check_zero_drift_current: hits=0
test_toolkit_sync_check_detects_upstream_drift: hits=0
test_toolkit_sync_check_reports_new_ps_commits: hits=0
test_toolkit_provenance_pinned_commit_valid: hits=0
test_toolkit_provenance_curation_decision_recorded: hits=0
test_toolkit_provenance_drift_audit_log_appendable: hits=0
test_ps_convergence_documentation_no_destructive_delete: hits=0
test_crucible_docker_preservation_window_documented: hits=0
test_toolkit_sync_check_requires_ps_root: hits=0
test_vendored_toolkit_no_new_float_money_math: hits=1
```

`grep -rln 'def test_vendored_toolkit_no_new_float_money_math' tests/` → `tests/engines/nautilus/test_toolkit_provenance.py`.

## Files touched

- `.forge/plans/2026-07/07-ps-shared-curation-and-convergence.md` (in-place fixes for all 10 findings)
- `.forge/handoff/2026-07/evidence-scout-07-08.md` (appended §11 errata for CR-8)
- `.forge/fixes/2026-07/07-plan-fix.md` (this fix log)

## Findings status summary

- **Fixed in place**: 10/10 (CR-1 through CR-10 all applied)
- **Partial / deferred**: 0
- **New ambiguity surfaced**: none — all 10 findings were narrow and directly actionable; no cross-cutting or scope escalation raised
- **Recommended next step**: L2 codex re-review for verdict confirmation (`--peer=codex` second-call within the max_calls_per_plan=3 budget). If L2 APPROVED → execute-team dispatch. Any residual finding triggers a targeted second in-place fix pass (Option B directed fix per Plan 06 precedent, not full re-draft).
